# 公開前の確認

このサイトは現在、ローカルだけにあります。リモートリポジトリは設定していません。初期公開は github.io URL・無広告で行い、独自ドメインは調査後に判断します。

## 1. 画面と動作を確認

WSLでこのディレクトリに移動し、次を実行します。

    python3 -m http.server 8000 --bind 127.0.0.1

Windows側のブラウザで http://localhost:8000/ を開きます。表示できない場合はWSL側ブラウザまたはWSLのポート転送設定を確認します。

- トップから3ページへ移動できるか。スマホ幅でも入力と結果を読めるか。
- ルーレット: ページを開いた時点で盤面が見えるか。「ランダム演出を有効化」をオンにして複数回試し、ひとつ進む・逆回転・マスの入れ替わり・盤面が割れる結果表示・揺れ戻しを確認する。オフでは通常の回転になるか。どの演出でも表示結果が最終的な針の位置（盤面が割れる演出では大きな結果表示）と一致するか。OSの「動きを減らす」が有効でも回転し、ページ内の回転チェックを外すと回転を省略できるか。2件、20件、空行、同じ候補、41文字の入力も試す。
- あみだくじ: 初期状態で3本のラインと「当たり」1件・「はずれ」2件が見えるか。2〜8本への変更で入力欄が増減し、横スクロールなしで全ラインが見えるか。上下の入力欄を直接編集し、取っ手のドラッグと左右矢印キーで位置を入れ替えられるか。増やすと新しい「はずれ」が入り、減らすと未編集の「はずれ」から消えるか。カバーの上下にラインが少し見え、生成後も中央は隠れたままか。「結果を表示」でカバーがゆっくり開くか。道筋のハイライトと表示結果が一致するか。公開後の編集でカバーが戻り、作り直せるか。
- サイコロ: 1個と10個を試す。各出目が1〜6で、合計が正しいか。
- キーボードだけでボタンを操作できるか。ブラウザの開発者ツールにエラーがないか。
- プライバシーページの説明を確認する。

## 2. 公開先と解析設定

- 公開先のGitHubアカウントとリポジトリ名を決める。初期公開時に独自ドメインは不要。
- プライバシーページに、実際に使う配信事業者を反映する。
- GA4のウェブデータストリームを作成し、assets/config.js に測定IDを設定する。
- GA4で tool_name をイベントスコープのカスタムディメンションとして登録すると、同じイベント名の3ツールを比較できる。入力値・結果値は送らない。
- 公開URLが確定したら、sitemap.xml を生成する。例:

      python3 scripts/make_sitemap.py https://ACCOUNT.github.io/REPOSITORY/

- Search Consoleに公開サイトを登録する。

## 3. 公開後の確認

- 全ページがHTTPSで開けるか。ブラウザの開発者ツールにCSP違反やJavaScriptエラーがないか。
- GA4のリアルタイム画面でページ閲覧と tool_generate / tool_reveal が確認できるか。
- Search Consoleで各ツールページが検出・登録されるか。時間がかかる場合がある。
- Search Consoleにsitemap.xmlを送信する。掲載順位や登録自体は保証されない。
- ツール別の閲覧、利用、再訪、検索表示・クリックを継続記録する。収益化を検討する際は、運用費用と収入見込みを別に試算する。

## 自動チェック

    node --check assets/app.js
    node --check assets/analytics.js
    node tests/smoke.cjs
    python3 tests/check_site.py

自動チェックは実ブラウザの見た目、GA4の実送信、Windows側の接続を代替しません。
